enabled: true
trace_path: .artifacts/observability/traces.jsonl
capture_content: false
langfuse:
  enabled: true
  base_url: http://host.docker.internal:3000
  public_key_env: LANGFUSE_PUBLIC_KEY
  secret_key_env: LANGFUSE_SECRET_KEY
  service_name: findociq
  timeout_seconds: 10

